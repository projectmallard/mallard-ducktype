@ducktype/1.0
@define foo bar

= Definition Test

The style attribute should be $em[style="$foo;"](bar).

The style attribute should be $em[style='$foo;'](bar).

The style attribute should be $em[style=$foo;](bar).

The style attribute should be $em[.$foo;](bar).
